/*Pascal Triangle*/
#include <stdio.h>

int main()
{
  int rows, coefficient = 1;
  printf("Enter the number of rows for Pascal's Triangle: ");
  scanf("%d", &rows);
  for (int i = 0; i < rows; i++)
  {
    
  }
  return 0;
}
