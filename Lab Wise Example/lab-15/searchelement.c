// 1. This is a C program that includes a main function, which is the entry point for a C program.
#include <stdio.h>

int main()
{
    // 2. This is the body of the main function.
    // 3. Inside the main function, we're declaring an integer variable called 'x' and initializing it with the value 5.
    int x = 5;

    // 4. We're printing the value of 'x' using the printf function.
    printf("The value of x is: %d\n", x);

    // 5. We're returning 0 from the main function. This indicates that the program has executed successfully.
    return 0;
}